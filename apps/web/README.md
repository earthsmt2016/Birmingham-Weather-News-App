# Frontend

`apps/web` is the React + Vite frontend.

It owns pages, components, hooks, styling, browser state, and calls to `/api/*`. It does not fetch third-party weather or news sources directly; that belongs in the backend.
